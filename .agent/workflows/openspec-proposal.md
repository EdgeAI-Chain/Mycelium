---
description: create a new change proposal
---
# OpenSpec Proposal

1. Create a new proposal
```bash
openspec new
```

2. Follow the interactive prompts to define your change.
3. The AI will generate a `proposal.md` and `tasks.md` in `openspec/changes/<id>/`.
